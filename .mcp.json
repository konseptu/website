{
  "mcpServers": {
    "playwright": {
      "command": "npx",
      "args": [
        "@playwright/mcp@latest",
        "--config",
        "./playwright-mcp.config.json"
      ],
      "env": {
        "NODE_ENV": "development"
      }
    },
    "postgres": {
      "command": "npx",
      "args": [
        "-y",
        "@modelcontextprotocol/server-postgres",
        "postgresql://vocafeuvre:lolomobanga@localhost:5432/sukify"
      ]
    }
  }
}
